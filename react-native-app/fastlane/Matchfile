#sync apple developer certificates and profiles
# match docs: https://docs.fastlane.tools/actions/match
git_url(ENV["MATCH_CERTIFICATES_REPO_URL"]) # url for certificates repo
storage_mode("git")
type("appstore") # The default type, can be: appstore, adhoc, enterprise or development
app_identifier(["com.transitastrology-"])

